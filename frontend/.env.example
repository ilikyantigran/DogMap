# Base URL for the DogMap REST edge/gateway (Go backend, see Docs/02-Backend.md).
# In dev, requests to /api are proxied by Vite (see vite.config.ts), so you can
# leave this as "/api". In production set it to the deployed gateway origin.
VITE_API_BASE_URL=/api
